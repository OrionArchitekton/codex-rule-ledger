# Synthetic Retry Recovery Fixture

Disclosure label: `SYNTHETIC_SANITIZED`.

This is a repository-authored, sanitized synthetic case. It is not a captured
Codex session and does not attest that any command or completion claim occurred.

The case deliberately exercises a different instruction topology from the
Build Week fixture: a selected global override, an empty root override that
occupies its project slot, a nested fallback, and a deeper selected
`AGENTS.md`. Its normalized evidence includes one successful requirement, one
conditional non-trigger, one missing requirement, and a failed command followed
by an exact successful retry before the completion claim. Subjective prose is
declined and an ambiguous either/or instruction is routed to human review.
